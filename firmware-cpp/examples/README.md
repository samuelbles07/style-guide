# Examples

These examples are small, illustrative snippets aligned with the rules in `firmware-cpp/`.

They are not a required architecture and are intentionally minimal so agents can fetch them quickly.

Files:

- `firmware-cpp/examples/spi_driver.h`
- `firmware-cpp/examples/spi_driver.cpp`
- `firmware-cpp/examples/uart_c_api.h`
- `firmware-cpp/examples/uart_c_api.c`
- `firmware-cpp/examples/heap_init.cpp`
